<div class="card">
  <div class="card-header">
    <h4 class="flex-row"><a class="link" href="/post/{{id}}">{{title}}</a><span class="user">Posted by {{user.username}} on {{format_date createdAt}}</span></h4>
    
  </div>
  <div>
    <p class="card-body">{{post_text}}</p>
  </div>
</div>